apiVersion: v1
kind: Service

metadata:
  name: receipt-ocr-api
  namespace: receipt-ocr

  labels:
    app: receipt-ocr-api

spec:

  type: LoadBalancer

  selector:
    app: receipt-ocr-api

  ports:

    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000